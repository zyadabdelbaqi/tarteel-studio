const { Worker } = require('worker_threads');
// Just checking if node has issues with string concatenation
console.log('bold ' + (35 * 0.66666) + 'px "Scheherazade New", "Noto Sans Arabic", sans-serif');
